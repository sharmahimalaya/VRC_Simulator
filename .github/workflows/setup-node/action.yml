name: "setup-node"
description: "Setup Node.js ⚙️ - Cache npm dependencies ⚡ - Install dependencies 🔧"

inputs:
  node-version:
    description: "Version of Node.js to use"
    required: false
    default: "20"

runs:
  using: "composite"
  steps:
    - name: Setup Node.js ⚙️
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: "npm"

    - name: Install dependencies 🔧
      shell: bash
      run: npm ci
